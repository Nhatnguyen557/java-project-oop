import java.awt.Color;
import java.awt.Graphics;

public class OrangeGhost {
    public void draw(Graphics g, int x, int y) {
        g.setColor(Color.ORANGE);
        g.fillArc(x, y, 28, 28, 0, 180);
        g.fillRect(x, y + 14, 28, 14);
    }
}
