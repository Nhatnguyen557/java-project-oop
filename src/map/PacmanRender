import java.awt.Color;
import java.awt.Graphics;

public class PacmanRenderer {
    public void draw(Graphics g, int x, int y) {
        g.setColor(Color.YELLOW);
        g.fillArc(x, y, 28, 28, 30, 300); // Hình miệng há
    }
}
